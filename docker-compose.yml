services:
  preflight-rs:
    build: .
    ports:
      - "8080:8080"
    environment:
      API_BEARER_TOKEN: "${API_BEARER_TOKEN:-change-me}"
      BIND_ADDR: "0.0.0.0:8080"
      RUST_LOG: "${RUST_LOG:-info}"
